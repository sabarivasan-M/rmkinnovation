{
  "name": "q-shield-blockchain",
  "version": "1.0.0",
  "description": "Local Hardhat blockchain layer for Q-Shield CryptoVault security decision audit logging.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.ts --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.4",
    "typescript": "^5.3.3",
    "ts-node": "^10.9.2"
  }
}
